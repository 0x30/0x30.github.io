import { defineComponent } from "vue";
import Style from "./index.module.scss";

export const ChatRoomButton = defineComponent(() => {
  return () => {
    return <button class={Style.chatRoomButton}>
      
    </button>;
  };
});
